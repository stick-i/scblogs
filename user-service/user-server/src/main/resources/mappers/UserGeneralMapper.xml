<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.sticki.user.mapper.UserGeneralMapper">

	<select id="updateViewNumByUserId" resultType="integer">
		update user_general
		set view_num = view_num+1 where user_id = #{userId}
	</select>

	<select id="updateLikeNumByUserId" resultType="integer">
		update user_general
		set like_num = like_num + #{num} where user_id = #{userId}
	</select>

	<select id="updateFansNumByUserId" resultType="integer">
		update user_general
		set fans_num = fans_num + #{num} where user_id = #{userId}
	</select>

	<select id="updateCommentNumByUserId" resultType="integer">
		update user_general
		set comment_num = comment_num + #{num} where user_id = #{userId}
	</select>

	<select id="updateCollectNumByUserId" resultType="integer">
		update user_general
		set collect_num = collect_num + #{num} where user_id = #{userId}
	</select>

	<select id="updateBlogNumByUserId" resultType="integer">
		update user_general
		set blog_num = blog_num + #{num} where user_id = #{userId}
	</select>

</mapper>